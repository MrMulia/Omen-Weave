import React from 'react';

const ShaDetections = () => {
  return (
    <div>
      <h1>SHA-based Detections Details</h1>
      {/* Add detailed content for SHA-based Detections here */}
    </div>
  );
};

export default ShaDetections;
